version: "3.8"

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: vehicle-booking-app
        ports:
            - "3000:3000"
        environment:
            - NODE_ENV=production
        restart: unless-stopped
        # Uncomment and configure if you have a database service
        # depends_on:
        #   - db
        # networks:
        #   - app-network

    # Uncomment if you need a PostgreSQL database
    # db:
    #   image: postgres:15-alpine
    #   container_name: vehicle-booking-db
    #   environment:
    #     POSTGRES_USER: ${POSTGRES_USER:-postgres}
    #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    #     POSTGRES_DB: ${POSTGRES_DB:-vehicle_booking}
    #   ports:
    #     - "5432:5432"
    #   volumes:
    #     - postgres_data:/var/lib/postgresql/data
    #   networks:
    #     - app-network
# networks:
#   app-network:
#     driver: bridge

# volumes:
#   postgres_data:
